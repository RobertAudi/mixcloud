<%= form_for @mix, multipart: true do |f| %>
  <p>
      <%= f.label :title, 'Title' %>
      <%= f.text_field :title %>
  </p>

  <p>
      <%= f.file_field :sound_file %>
  </p>

  <p><%= f.submit "submit" %></p>
<% end %>
